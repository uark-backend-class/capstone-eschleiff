extends layout 

block content 
    .card
        form(action='/login' method='POST')
            .container
                h1.login Login 
                p Please enter your email and password to login.
            
                hr

                label(for='email') #[strong Email]
                input#email(type='text' placeholder='Enter Email' name='username' required)

                label(for='psw') #[strong Password]
                input#psw(type='password' placeholder='Enter Password' name='password' required)

                hr

                button.registerbtn(type='submit') Login

